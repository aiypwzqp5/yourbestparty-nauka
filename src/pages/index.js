import * as React from "react";
import GlobalStyle from "../styles/GlobalStyles";

const IndexPage = () => {
  return (
    <main>
      <GlobalStyle />
    </main>
  );
};

export default IndexPage;
